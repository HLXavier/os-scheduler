Integrantes:

    - Fernanda Rosa
    - Henrique Xavier
    - Lucas Antunes
    - Vinicius Saliba


Instruções:

    --- Python 3.8.10 ---

    - os arquivos teste devem ficar dentro 
    da pasta "cases", no root do projeto

    - não deixe linhas em branco nos casos
    de teste

    - as operações (IN, OUT) devem ser escritas
    em maiúsculo

    - os comandos tem o seguinte formato:
    IN(<pid>, <tanho>)
    ou
    OUT(<pid>)


Para rodar:

    $ python3 main.py <arquivo> <espaço> <fit> 

    ou (opcional, com descrição completa da memória)

    $ python3 main.py <arquivo> <espaço> <fit> verbose    

    exemplos:
    $ python3 main.py case1 16 ff
    $ python3 main.py case2 32 cf verbose


    fits:
        ff -> first-fit
        bf -> best-fit
        wf -> worst-fit
        cf -> circular-fit
    